# Logistics operations — independent story gate and domain criteria

Date: 2026-09-19. Verdict: **changes**. One CRITICAL cross-story responsibility conflict remains. The targeted earlier context/workflow fixes are resolved in the reviewed text. The twelve domain acceptance criteria below are independently authored proposals for PM challenge, not implementation tests or evidence of production readiness.

Reviewed [operational App Spec](../2026-09-19-app-spec-logistics-operations.md) at `87fd3543614ae88ba648e0df1efe56acfa05f4f8`, primarily §§5–6, using §§1–3.5 and 7 as context. All App Spec line references below refer to that snapshot. Applied the supplied `om-app-spec-writing/references/challenger-prompt.md`. Read the [author resolutions](logistics-operations-review-resolutions.md), [context re-review](challenger-logistics-operations-context-r2.md), [workflow review](challenger-logistics-operations-workflows.md) and [commit ledger](logistics-operations-commits.md). No broad re-review of already resolved context findings was commissioned or performed.

## CRITICAL — S-C1: source-change detection has no consistent read/write boundary

**Locations:** §1.4 invariant 7, line 135; §2 action grants, lines 173 and 181; US06/US15, lines 387 and 411; impact matrix US06/US15 and event rule, lines 426, 435 and 438; A14/A15 GET contracts, lines 568–569; master-race verification, line 599.

The invariant says authoritative board load/refresh detects source changes and, after departure, “opens a disruption.” The new US15 impact row says navigation has no server domain mutation. Opening an ordinary disruption requires `logistics.execution.manage`, yet the dashboard is available to a read-only operator. Events are explicitly limited to invalidating views/indexes; they cannot supply an unspecified domain writer instead.

**Counterexample:** an active trip has a recorded eligible observation. A planner user inserts an unavailable rule. An operator with logistics read access and the applicable source read grants, but no execution grant, refreshes the board. Detection must occur even if the source-change event was lost. If that GET persists a disruption, it violates the read-only/no-navigation-write contract and leaves actor/authorization unclear. If it only renders a warning, the promised persisted disruption has not been opened. Two readers or periodic refreshes also need a defined duplicate boundary if detection is intended to write. Source snapshot consistency is not the problem; ownership of the reaction is.

**Minimal fix:** distinguish a derived source-change exception from a persisted Disruption. Board/selector GETs expose the changed/unknown observation and a visible action-needed state without writing. Name an authorized, versioned, idempotent logistics command that records the disruption, and its UI entry point; this can use the existing disruption action with typed source-change context. Keep the exception visible until addressed, explicitly revise the automatic-opening wording, and retain custody/reservations throughout. Alternatively specify a separately authorized writer if automatic persistence is essential, but do not hide it inside a GET or an invalidation subscriber. The manual command option needs no new automation subsystem.

**Required trace:** insert the rule after start; lose the event; refresh as read-only and verify a visible exception with zero domain writes. An authorized operator records the disruption; duplicate/concurrent requests produce one effective disruption for that detected change, with a recorded actor and current scope/permissions. Repeat refreshes do not recreate a resolved disruption for the identical acknowledged observation; a genuinely new change remains detectable. No cargo or occupancy is automatically released. Update US06/US09/US15, their matrix responsibility and LOG-OP-14/15/master-race coverage consistently.

## WARNING — S-W1: make both metric denominators explicit

**Locations:** §1.4 comparison predicate, line 145; US14, line 407; LOG-OP-13, line 594.

US14 already rejects a “zero denominator,” but the detailed predicate names only positive total distance. A complete baseline with E=0 and T=100 has a valid 0% empty share while relative reduction divides by that zero share. A pilot E=0/T=100 must not become an achieved improvement; E=10/T=100 must not produce infinity. Clarify the relative-change formula and require positive baseline empty share for that calculation. Keep the two valid period ratios and an explanatory N/A relative result. This is a narrow precision warning, not a request to exclude loaded-only days, change the cohort or introduce a new metric system. Criterion DA11 supplies concrete expected values for PM challenge.

## Targeted earlier fixes — independently checked

| Earlier finding | Trace in revised text | Disposition |
|---|---|---|
| Context R2-C1 interruption | Line 123 rejects source interruption with onboard A transferred but unpicked B still assigned. Release/cancel B closes membership/skips stops first, or the combined operation rolls back. The no-cargo-only source is explicitly covered. | Resolved as a specification rule; implementation proof remains outstanding. |
| Historical attempts / handover warning | Line 125 limits new promise validation to pending effective obligations and explicitly defines handover custody/mileage boundaries; target delivery needs no fabricated second pickup. | Resolved. |
| Workflow F1 missing reading | Line 234 says a valid delivery/return ends custody without an odometer; lines 115 and 391 permit physical finish with incomplete mileage. | Resolved; missing mileage is not missing custody. |
| Workflow F2 independent boundaries | Lines 198 and 251 separate resource readiness from job acceptance, and physical recovery from correction. Correction rejection preserves facts and links conflicting records without inventing an investigation subsystem. | Resolved; the more specific subflow contracts govern the group summaries. |
| Workflow F3 intake ROI | Lines 196–198 remove the unsupported numerical follow-up target and treat validation completeness as operational input. | Resolved; no unobserved saving is claimed. |
| Workflow F4 overdue obligations | Line 305 retains one current pickup/delivery obligation, supersedes failed attempts explicitly, excludes settled history and gives return/handover/end a separate plan-time exception. Monday failure → Tuesday retry → success cannot leave two overdue rows. | Resolved. |
| Workflow F5 entry/repair boundaries | Line 307 names populated-board Create job, ready-job Plan trip, retained queue filters and execution-only first-reading append on trip detail. Existing-reading corrections and non-trip/day repairs point to authorized manager surfaces. | Resolved; no manager grant is needed merely to append the first stop reading. |

These dispositions verify author claims against the committed text; they do not mark the whole context, workflow, rollout or implementation gate passed.

## Every-story path and impact audit

All sixteen stories have an identified internal actor, named UI surface, happy path, valid alternate and failure outcome. Line 369 supplies the common unchanged-state rule and distinguishes an unknown post-commit outcome. Scope/features in §2 apply even where the story uses a role name as shorthand. The following are manual specification traces, not executed transport workflows.

| Story / lines | Happy and alternate state traced | Failure, identity and cross-story result |
|---|---|---|
| US01 / 373 | Fleet forms save one profile per scoped master; disable preserves history/cohort. Planner unavailable permits the profile, not invented eligibility. | Duplicate/cross-scope/stale saves create no orphan. Fleet grant and source-read boundaries are explicit; US04/06 must revalidate later eligibility. |
| US02 / 375 | Job create/detail accepts once to ready, or retains a complete draft. A duplicate customer reference can describe distinct work after acknowledgement. | Invalid/stale/revoked input does not accept; lost response uses original request. Unsaved abandonment creates nothing, saved draft stays discoverable. Acceptance is independent of US01 capacity. |
| US03 / 379 | Trip detail saves ordered draft/load preview; estimates can remain absent. | Changed candidate/invalid order preserves draft, never books or changes jobs. Abandonment has no US04/10 custody or reservation effect. |
| US04 / 381 | Confirmation commits booking, memberships, snapshots and receipt together; adjacent intervals remain valid if feasible. | Competing vehicle, driver or job requests serialize, loser retains draft. Altering another booking still requires its own version/authorized replan under US05. Replay cannot double-book. |
| US05 / 383 | Replan replaces atomically; pre-start cancellation returns unpicked jobs; active unpicked release skips remaining stops. | Failed replacement retains original booking. Onboard release is denied. Assigned promise edits need dispatch plus job features; physical facts cannot be rewritten by plan edits. |
| US06 / 387 | Departure starts occupancy; a truthful late departure remains a late fact. | Active prior trip/unsupported source/revocation blocks start. Source race remains disclosed. **S-C1** affects the later discovery/reaction, not the strong occupancy lock. |
| US07 / 389 | Stop fact changes full-job custody once; incoming leg retains previous cargo state. Late/missing-reading reports remain truthful. | Invalid sequence/reference/future time/conflicting duplicate has no cargo effect. Idempotent replay and fact/ledger versions protect US08–14. Source/recorder identities are distinct. |
| US08 / 391 | Dispatcher finishes a trip with settled obligations/end; all unpicked jobs may instead have been released. Mileage can remain incomplete. | Onboard cargo prevents finish; historical released jobs do not. Ordinary finish belongs to execution authorization; independently releasing recovery resources uses §2's manager feature combination. |
| US09 / 395 | Retry keeps failed attempt/original promises; return stays loaded until actual handback; never-collected work may be released/cancelled. | Unagreed promise/conflicting reservation/onboard cancellation is rejected, disruption stays open. Abandoned dialog changes no custody. Shared commands enforce extra dispatch/job grants when changing plans/promises. **S-C1** needs a defined source-change entry. |
| US10 / 397 | Draft receiver obtains custody only on atomic handover/start; same-driver boundary release/reacquire is valid. | One invalid job or competing delivery/transfer rolls back the selected set. Subset transfer leaves a valid source driver/plan; source terminal predicates still settle every unpicked obligation. Manager combined features apply. |
| US11 / 399 | Manager evidence-backed replacement/void leaves one effective successor and original history. | Concurrent successor or later-history contradiction rejects without indirect edits to another trip. Reconciliation invalidation and recomputation are atomic consequences, not event-dependent repair. |
| US12 / 403 | Manager fills non-trip movement/evidenced gaps; unknown load is a valid explicit interim state. Execution-only users can append a first stop reading. | Reversed/overlapping/stale/estimated entries cannot fabricate verified distance. Trip load cannot be freely reclassified; unknown outside cargo retains provenance. |
| US13 / 405 | Exact day coverage reconciles; evidenced no-motion and distance-complete/unknown-load days remain distinct. | Missing midnight evidence/reset/overlap/stale ledger prevents false completeness. Corrections in US11 invalidate previous review, including otherwise completed trips. |
| US14 / 407 | Frozen cohort comparison and separate whole-fleet view preserve membership and revisions. | Incomplete/unknown/zero-distance results cannot claim improvement; late correction revises results. **S-W1** clarifies zero baseline share. Configuration uses measurement grant, not general viewing. |
| US15 / 411 | Readers use scoped board/drill-through; authorized actions have keyboard/mobile entry. | Failed refresh retains stale data; old-organization responses are ignored. Counts cover all pages. **S-C1** is the missing boundary between read-only detection and persisted disruption. |
| US16 / 413 | Existing authorized access editor grants exact/wildcard features or a custom role name. | Missing admin authority/stale edit fails; revoked effective rights block the next write/replay. Drivers/customers need no app identity. No promise of erasing already rendered browser content. |

### Matrix, gap mapping and bounded reactions

Every US01–US16 appears once in the impact matrix and once in §6's capability mapping. Custody/booking/correction writes are synchronous, serialized domain commands. Events follow committed truth, refresh/index only, and replay produces no duplicate domain notifications; a lost event therefore cannot undo a committed fact. Aside from S-C1's undefined discovery reaction, no required event-to-command cascade is present.

Representative conflicting pairs are accounted for: US04 versus US04 competes on resource/job keys; US05 versus US07 serializes membership/plan/custody; US07 versus US10 competes for the same onboard job; US10 versus US10 cannot transfer twice; US11 versus US11 cannot branch a fact; US11 versus US13/14 invalidates stale reconciliation/comparison; US16 revocation applies to both fresh writes and outcome replay. UI projections can lag, but the next command revalidates authoritative state. Post-commit response/event failure is a receipt-reconciliation problem, not a rolled-back shipment.

The gap analysis retains existing masters, ACL, forms, schedule rendering and commands; owned transport obligations, exclusivity, custody and odometer reconciliation remain app work. The original union was C01–C25, with shared items deduplicated. During review, parent revision `01dafdd3` refined these into 25 work packages / 33 estimated atomic commits: 19 unsplit packages plus 14 increments across six split packages. The revised raw workflow total 36 minus shared C09 (2) and C17 (1) equals 33. C16/C18 now explicitly have three increments each. The revision preserves all sixteen story mappings and the single complete release; it does not resolve S-C1 or change the targeted earlier fixes. This review verifies domain-to-story coverage, not the independent architect checkpoint's source-adapter implementation claims. No new shared framework, provider, GPS, routing certification, portal or split-load subsystem is needed to fix S-C1/S-W1.

## Independently authored release domain acceptance criteria — PM to challenge

**Role reversal:** the reviewer wrote DA01–DA12 below from domain outcomes, including falsifying counterexamples. They cover the complete first release, not separate releasable fragments. All remain **proposed and unexecuted**. DA03 incorporates the proposed S-C1 boundary; DA11 makes S-W1 explicit. The PM must accept or challenge those proposals before treating them as normative. The implementation must later supply actual API/UI/transaction evidence; a fixture calculation is not proof of real fleet improvement.

1. **DA01 — Admit real work without inventing capacity.** A scoped dispatcher can accept a complete customer job into ready even when no truck is free. Palletized cargo requires known positive pallet count; profile capacity and source identity belong to existing scoped masters. A complete saved draft remains unaccepted and discoverable; abandoning unsaved input creates no record. Duplicate submission creates one job, whereas an acknowledged repeated customer reference can represent a separate job. No driver login or new customer registry is required. Trace: US01–02, WF1a/b; LOG-OP-01/02/14.

2. **DA02 — Reserve a feasible whole-load plan exactly once.** A two-job trip includes pickup-before-delivery, intermediate weight/pallet checks, service/travel/return time and a final end. Two simultaneous confirmations sharing either job, vehicle or driver yield at most one winner and no partial losing reservation. Adjacent nonzero intervals can meet; zero-duration bookings cannot. Failed replan leaves the previous assignment intact. Abandoned draft, blank estimates and cancelled unsaved changes never reserve capacity. Trace: US03–05; LOG-OP-03/04/05.

3. **DA03 — Detect changed eligibility without changing physical truth through a read.** Unknown/unreadable/unsupported source availability prevents new confirmation/start/handover, and stored observations expose their checked interval/time/versions. A master edit after observation remains an acknowledged race. Read-only refresh after a lost change signal displays the new exception without writing or releasing anything; an explicitly authorized idempotent action records its disruption. An already-started trip can still report delivery/return/finish from persisted snapshots during source outage. Trace: US01/06/09/15 and S-C1; LOG-OP-01/14/15 plus master-race case.

4. **DA04 — Separate custody, physical completion and mileage completeness.** After departure, pickup makes the whole job onboard, valid delivery/return ends that custody once, and the incoming leg uses the cargo state before the stop. Delivery without an odometer still ends custody; finishing with a recorded end and settled obligations releases ordinary occupancy while mileage stays explicitly incomplete. Outstanding cargo prevents finish; missing reading alone does not. Active occupancy blocks another trip even beyond the planned end. An execution-only operator can append the first missing stop reading but cannot alter an existing one as a free edit. Trace: US06–08/12, F1/F5; LOG-OP-06/08/12.

5. **DA05 — Preserve failed attempts and customer promises during recovery.** A failed Monday delivery leaves cargo onboard and preserves its original promise/attempt. An agreed Tuesday retry creates a linked effective attempt; Monday is not revalidated against Tuesday's window. Success settles the obligation and leaves neither attempt overdue. Choosing return retires the delivery obligation, records return as loaded movement until handback, and measures return lateness against its plan rather than a customer delivery promise. Unagreed windows and cancellation of onboard cargo fail unchanged. Trace: US05/09/15, F4; LOG-OP-05/09/15.

6. **DA06 — Transfer custody atomically and settle the source.** For source S carrying A and awaiting pickup of B, a valid own-fleet recovery target receives all of A in one atomic handover/start with one custodian; it does not fabricate pickup/delivery. S cannot interrupt until B is explicitly released or cancelled and S's physical end/no-custody predicate holds. With two selected onboard jobs, one invalid job rolls the entire transfer back. Same-driver recovery ends and reacquires driver occupancy at one boundary; source vehicle release is separate and a broken vehicle remains disabled until manager re-enablement. Trace: US10/08/05; LOG-OP-08/10 and R2-C1 case.

7. **DA07 — Repair reports without rewriting another journey.** An authorized evidenced replacement or void retains the original fact, recorder and reason and has one effective successor. Two competing corrections cannot branch history. A proposed removal of pickup supporting a later delivery/handover is rejected with the conflicting record and no partial consequences. A valid reading/time repair atomically recomputes affected custody/mileage and invalidates affected day reviews/comparison results. New real movement must use execution/recovery, never a correction masquerading as transport. Trace: US11/12–14; LOG-OP-11/13.

8. **DA08 — Resolve uncertain outcomes without repeating work.** Lose the HTTP response or post-commit event after confirmation, pickup or handover. Authorized replay of the same request/body or outcome lookup returns the committed record identities rather than a second action; changed input with the same key conflicts. Failed precommit validation yields no successful receipt. Revocation/scope changes also apply to lookup/replay. Closing the browser before submission causes no domain mutation; after an uncertain submission the reopened workflow reconciles the original request, without claiming rollback or creating a fresh duplicate. Trace: US02/04/07/10/16; LOG-OP-07/14.

9. **DA09 — Account for every vehicle-day and every kilometre once.** A frozen cohort includes positioning, workshop, return and zero-job days. For an evidenced day envelope, effective non-overlapping legs satisfy E+L+U=T; multiple onboard jobs count distance only once. A missing day is not zero movement; an evidenced equal-opening/closing no-motion day contributes T=0. Unknown-load distance may be distance-reconciled but cannot authorize improvement. Midnight/DST boundaries require evidence; absent evidence, odometer reset, overlap or unrecorded gaps leave the relevant completeness visibly unresolved without prorating or guessing. Trace: US12–13; LOG-OP-12.

10. **DA10 — Keep the comparison population and corrections honest.** Freeze explicit vehicles, timezone and ordered baseline/pilot dates before baseline collection. Disabling a vehicle or encountering missing/reset data never removes it from the frozen denominator. A changed cohort creates a new comparison; the whole-fleet live view stays separately labelled. Every result identifies periods, membership, coverage, known totals and revision time; a late baseline correction recomputes and can withdraw a prior improvement indication. Estimates and demo fixtures never become actual fleet evidence. Trace: US01/11/14, §7; LOG-OP-13.

11. **DA11 — Calculate only mathematically defined improvement.** For complete fixed-cohort periods with T>0 and U=0, baseline E=250/T=1000 and pilot E=450/T=2000 give 25% and 22.5%, a 10% relative reduction, not a 2.5% relative reduction. Use `100 * (baselineShare - pilotShare) / baselineShare`; a worsening result remains negative. Either T=0, incomplete days or unknown load suppresses improvement. Baseline E=0 with positive T permits its valid 0% share but produces N/A relative reduction, whether pilot E is zero or positive. No earned-target badge follows an undefined calculation. Real target achievement remains an observed pilot outcome. Trace: US14, S-W1; LOG-OP-13.

12. **DA12 — Operate the complete manual loop through authorized, truthful surfaces.** Read-only, dispatcher and manager accounts see only permitted scoped data/actions; exact/wildcard grants, cross-org references, revocation and direct API access produce the same authorization result. The populated dashboard offers authorized intake/planning, includes yesterday's active trip, and has count/drill-through agreement beyond 100 jobs. Failure displays stale/error state rather than zero; delayed old-scope responses never replace the new organization. Primary navigation remains within the stated three clicks, with keyboard/mobile alternatives and five locales. All seven destinations remain accessible under their existing read contracts; Map/proposals remain honestly planned where excluded. A release completes intake through execution, recovery, correction and measurement together; rollback preserves facts and supports finishing/recovering in-flight work. Trace: US15–16 plus all release workflows; LOG-OP-14/15/16 and §7 rollout evidence.

## Evidence, validation and limits

Runner: **local**, documentation-only Git/PowerShell checks. At dispatch, the fork point and named parent both equalled `87fd35436`, so the fork-to-parent diff was empty. Read the introducing story/gap/API/coverage revision `ff17b324a^..ff17b324a` and targeted resolution/release revision `3e9e3d9e1..9baef8e8f`, rather than treating an empty diff as an empty review. Parent first added an architect note, then advanced to `01dafdd3ad56dbd61c9587c1b11a02685c707a24` with source-adapter detail, estimate splits and author resolutions. Read that fork-to-parent delta as a late scope check; its revised gap accounting is assessed above. The added adapter conformance and separate architect verdict are not certified by this story review. Original snapshot line references remain intentional.

`git diff --check ff17b324a^..87fd35436` passed with no output. Required-section, sixteen-story/path/matrix/gap coverage, twelve-criteria and local Markdown target checks were run for this review; the staged and committed note diff checks are recorded in the task report. No root author report exists in the tree's root files; author claims were taken from the committed resolutions and checked against their actual revisions.

No application unit/integration suite or build was run: this delta is a proposed specification and review note, and its own line 547 says operational entities/APIs/pages do not yet exist. LOG-OP references above identify required future evidence, never tests claimed passed. Only this note is committed. No code/main-spec edits, recursive skills, children, tracker writes, pushes or migrations. The review assignment is complete; the story gate remains **changes** until S-C1 is resolved, and independently authored criteria remain pending PM challenge.
